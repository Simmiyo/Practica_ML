 empirical size trained neural network relu neural network define piecewise linear function input however initialize train neural network different fit linear spline paper expand empirically upon previous theoretical work demonstrate feature trained neural network standard network initialization training produce network vastly simple naive parameter count would suggest impart odd feature trained network however also show forced simplicity beneficial indeed critical wide success network