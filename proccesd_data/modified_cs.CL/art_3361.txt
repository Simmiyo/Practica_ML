 left-center-right separate neural network aspect-based sentiment analysis rotatory attention deep learning technique achieve success aspect-based sentiment analysis recent year however two important issue still remain study i.e. efficiently represent target especially target contain multiple word utilize interaction target leave right context capture important word paper propose approach call left-center-right separate neural network rotatory attention lcr-rot good address two problem approach two characteristic three separate lstms i.e. leave center right lstms correspond three part review leave context target phrase right context rotatory attention mechanism model relation target leave right context target context attention use capture indicative sentiment word left right context subsequently context target attention use capture important word target lead two-side representation target left-aware target right-aware target compare approach three benchmark datasets ten relate method propose recently result show approach significantly outperform state-of-the-art technique