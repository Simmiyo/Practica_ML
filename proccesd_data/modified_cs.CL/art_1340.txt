 discriminative acoustic word embeddings recurrent neural network-based approach acoustic word embeddings -- fixed-dimensional vector representation variable-length spoken word segment -- begin consider task speech recognition query-by-example search embeddings learn discriminatively similar speech segment correspond word dissimilar segment correspond different word recent work find acoustic word embeddings outperform dynamic time warp query-by-example search related word discrimination task however space embed model training approach still relatively unexplored paper present new discriminative embedding model base recurrent neural network rnns consider train loss successful prior work particular cross entropy loss word classification contrastive loss explicitly aim separate same-word different-word pair siamese network train setting find classifier-based siamese rnn embeddings improve previously report result word discrimination task siamese rnns outperform classification model addition present analysis learned embeddings effect variable dimensionality network structure