 neural lattice-to-sequence model uncertain input input neural sequence-to-sequence model often determine up-stream system e.g word segmenter part speech tagger speech recognizer up-stream model potentially error-prone represent input word lattice allow make uncertainty explicit capture alternative sequence posterior probability compact form work extend treelstm tai et al. latticelstm able consume word lattice use encoder attentional encoder-decoder model integrate lattice posterior score architecture extend treelstm 's child-sum forget gate introduce bias term attention mechanism experiment speech translation lattice report consistent improvement baseline translate either -best hypothesis lattice without posterior score